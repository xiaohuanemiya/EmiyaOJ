<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.emiyaoj.service.mapper.PermissionMapper">

    <select id="findByUid" resultType="com.emiyaoj.service.domain.pojo.Permission"
            parameterType="java.lang.Long">
        SELECT p.* FROM permission p
        LEFT JOIN role_permission rp ON p.id = rp.permission_id
        LEFT JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{uid}
        GROUP BY p.id
    </select>
    <select id="findCodeByUid" resultType="java.lang.Integer" parameterType="java.lang.Long">
        SELECT p.permission_code FROM permission p
        LEFT JOIN role_permission rp ON p.id = rp.permission_id
        LEFT JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{uid}
        GROUP BY p.id
    </select>
</mapper>
