<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.emiyaoj.service.mapper.RolePermissionMapper">

    <!-- 根据角色ID查询权限ID列表 -->
    <select id="selectPermissionIdsByRoleId" resultType="java.lang.Long">
        SELECT rp.permission_id
        FROM role_permission rp
        INNER JOIN permission p ON rp.permission_id = p.id
        WHERE rp.role_id = #{roleId}
          AND p.status = 1
          AND p.deleted = 0
    </select>

    <!-- 根据角色ID删除角色权限关联 -->
    <delete id="deleteByRoleId">
        DELETE FROM role_permission WHERE role_id = #{roleId}
    </delete>

    <!-- 批量插入角色权限关联 -->
    <insert id="batchInsert">
        INSERT INTO role_permission (role_id, permission_id, create_time, create_by)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId}, #{item.permissionId}, #{item.createTime}, #{item.createBy})
        </foreach>
    </insert>

    <!-- 根据权限ID查询角色ID列表 -->
    <select id="selectRoleIdsByPermissionId" resultType="java.lang.Long">
        SELECT rp.role_id
        FROM role_permission rp
        INNER JOIN role r ON rp.role_id = r.id
        WHERE rp.permission_id = #{permissionId}
          AND r.status = 1
          AND r.deleted = 0
    </select>

    <!-- 根据角色ID列表查询权限ID列表 -->
    <select id="selectPermissionIdsByRoleIds" resultType="java.lang.Long">
        SELECT DISTINCT rp.permission_id
        FROM role_permission rp
        INNER JOIN permission p ON rp.permission_id = p.id
        WHERE rp.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        AND p.status = 1
        AND p.deleted = 0
    </select>

</mapper>
